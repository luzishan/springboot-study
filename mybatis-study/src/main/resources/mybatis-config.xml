<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<!--配置添加的顺序，顺序不能错，不然配置文件报错-->
<!--properties?,settings?,typeAliases?,typeHandlers?,objectFactory?,objectWrapperFactory?,
reflectorFactory?,plugins?,environments?,databaseIdProvider?,mappers?-->
<configuration>
    <properties resource="db.properties">
        <property name="password" value="123123"/>
        <!--<property name="org.apache.ibatis.parsing.PropertyParser.enable-default-value" value="true"/>--> <!-- 启用默认值特性 -->
    </properties>
    <!--<typeAliases>
        &lt;!&ndash;<typeAlias type="com.lu.domain.User" alias="user"/>&ndash;&gt;
        <package name="com.lu.domain"/>
    </typeAliases>-->
    <settings>
        <setting name="logImpl" value="LOG4J"/>
        <setting name="lazyLoadingEnabled" value="true"/>
        <setting name="aggressiveLazyLoading" value="false"/>
    </settings>
    <!--配置数据源-->
    <environments default="dev">
        <environment id="dev">
            <!--事物管理器-->
            <transactionManager type="JDBC"></transactionManager>
            <!--数据库连接池-->
            <dataSource type="POOLED">
                <!--<property name="driver" value="com.mysql.jdbc.Driver"/>
                <property name="url" value="jdbc:mysql://localhost:3306/mybatis_plus?useSSL=false&amp;useUnicode=true&amp;characterEncoding=utf8"/>
                <property name="username" value="root"/>
                <property name="password" value="123456"/>-->
                <property name="driver" value="${driver}"/>
                <property name="url" value="${url}"/>
                <property name="username" value="${username}"/>
                <property name="password" value="${password}"/>
            </dataSource>
        </environment>
    </environments>

    <!---->
    <mappers>
        <!--<mapper resource="com/lu/mapper/UserMapper.xml"/>
        <mapper resource="com/lu/mapper/StudentMapper.xml"/>
        <mapper resource="com/lu/mapper/CourseMapper.xml"/>-->
         <package name="com.lu.mapper"/>
    </mappers>
</configuration>